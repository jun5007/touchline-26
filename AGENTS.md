# TOUCHLINE 26 작업 원칙

- 시작 화면부터 결과 리포트까지 이어지는 완전한 사용자 흐름을 가장 우선한다.
- 미완성 기능을 많이 추가하지 않고, 한 개의 실제 경기와 미션을 끝까지 완성한 뒤 확장한다.
- 실제 경기·선수·스코어·교체 시점을 임의로 만들지 않는다.
- 샘플 데이터는 `isSample: true`로 분리하고 화면에 `DEMO DATA`를 명확히 표시한다.
- 능력치·상황 적합도·위험·영향 계산 로직은 UI 컴포넌트와 분리한다.
- 외부 API 키, 회원가입, 결제, 별도 설치 없이 핵심 기능이 동작해야 한다.
- Football Manager/FMM의 UI·문구·자산과 FIFA의 로고·엠블럼·공식 그래픽을 복제하지 않는다.
- 결과에는 선택의 장점과 위험 요소를 모두 출력한다.
- 실제 감독의 선택을 정답으로 단정하거나 선수·팀·국가를 비하하지 않는다.
- 확인된 사실과 전술적 추론을 데이터와 화면에서 구분한다.
- 변경 후 `npm run lint`, `npm run test`, `npm run build`를 실행한다.
- 최종 제출 마감 이후에는 GitHub 저장소에 커밋하지 않는다.

